# Infrastructure services shared by all development scenarios
#
# Scenario 1 (API local):    docker compose -f compose.base.yml -f compose.idp.yml up -d
# Scenario 2 (all Docker):   docker compose -f compose.base.yml -f compose.idp.yml -f compose.api.yml up -d
# Scenario 3 (API+IDP local): docker compose -f compose.base.yml up -d

services:
  api-sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: hmm-api-sqlserver
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: ${API_SA_PASSWORD}
      MSSQL_PID: "Developer"
    ports:
      - "${API_SQLSERVER_PORT}:1433"
    volumes:
      - api-sqlserver-data:/var/opt/mssql
    networks:
      - hmm-network
    healthcheck:
      test: /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "${API_SA_PASSWORD}" -C -Q "SELECT 1" || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped

  seq:
    image: datalust/seq:latest
    container_name: hmm-seq
    environment:
      ACCEPT_EULA: "Y"
      SEQ_FIRSTRUN_NOAUTHENTICATION: "true"
    ports:
      - "${SEQ_INGESTION_PORT}:5341"
      - "${SEQ_UI_PORT}:80"
    volumes:
      - seq-data:/data
    networks:
      - hmm-network
    restart: unless-stopped

networks:
  hmm-network:
    driver: bridge

volumes:
  api-sqlserver-data:
  seq-data:
